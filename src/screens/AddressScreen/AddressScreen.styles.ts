// StyleSheet for AddressScreen — built from theme tokens.

import { StyleSheet } from 'react-native';

export const addressScreenStyles = StyleSheet.create({
  // Lets the two footer buttons share the row evenly (Button owns no width).
  footerButton: {
    flex: 1,
  },
});
