// StyleSheet for PersonalInfoScreen — built from theme tokens.

import { StyleSheet } from 'react-native';

export const personalInfoScreenStyles = StyleSheet.create({
  // Lets the single footer button fill the row (Button owns no width).
  footerButton: {
    flex: 1,
  },
});
